# Project Map

## Overview

This repository contains a demo Android project (android-supabase-todolist) demonstrating Supabase auth + todo CRUD, and a target Android app under src/app which is the main application to integrate features into.

## Main Areas

- demo/android-supabase-todolist/
  - Purpose: Minimal demo showing Supabase authentication and a Todo list CRUD example.
  - Key files: build.gradle.kts (root), local.properties.example, src/ (Android app source)

- src/app/
  - Purpose: Target application that will receive features from the demo. Contains app module source and resources.
  - Key files: build.gradle.kts, src/ (Kotlin sources, AndroidManifest, res)

- Root-level build files
  - settings.gradle.kts and build.gradle.kts at repository root affect how modules are included and shared versions.

## How to use this map

1. The Explorer agent will use this map as the canonical structure while analyzing code.
2. If files or directories are added/removed, update this PROJECT_MAP.md accordingly.

## High-Risk Areas

- Dependency versions (Supabase, PowerSync, Compose) may conflict between demo and app.
- Packages/namespaces must be consistent when merging code.
- Android SDK and Gradle plugin versions must be compatible.

## Next Steps

- The Explorer will list files under each module and read the requested files (manifests, Kotlin source files, layouts).
- The Explorer will not reproduce entire files blindly; it will provide concise, necessary fragments (method signatures, class headers, key code excerpts) and precise file paths per project rules.


(Generated by Explorer skill)
